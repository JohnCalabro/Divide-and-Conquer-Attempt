function sortedFrequency(arr, num) {
    let count  = 0;
    const mid = Math.floor((arr.length -1)/2)

    // trying to implement binary search 

    //am unable - all of these are impossible at the moment. to save 
    
    //time, will revisit for now will put up first commit
    /
    console.log(mid)
}

sortedFrequency([1,1,2,2,2,2,3],2) 


module.exports = sortedFrequency

//> search to right